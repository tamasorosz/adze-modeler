[tox]
envlist = py3
isolated_build = True

[testenv]
deps =
    -rrequirements.txt
    exdown
    pytest
    pytest-cov
extras = all
commands =
    pytest --cov {envsitepackagesdir}/artapsegment --cov-report xml --cov-report term