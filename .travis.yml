language: python
cache: pip

python:
    - "3.8"

sudo: false

install:
    - pip install -U pip
    - pip install -U pytest
    - pip install coverage tox-travis
    - apt install python3-gmsh
    - pip install pygmsh
script:
    - tox

after_success:
    - pip install codecov
    - codecov -e TOXENV,DJANGO

notifications:
email: false